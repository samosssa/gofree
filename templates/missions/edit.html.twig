{% extends 'base.html.twig' %}

{% block title %}Edition mission{% endblock %}


{% block body %}

    <div class="container">
        <h1>Modifier l'annonce : {{ miss.title }}</h1>

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Sauvegarder</button>

        {{ form_end(form) }}

    </div>

{% endblock %}


